# dependencies
node_modules
.pnpm-store

# build outputs
dist
build
.next
out
.expo
.expo-shared

# Turbo
.turbo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# Supabase CLI local cache
supabase/.temp

# environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS files
.DS_Store
Thumbs.db

# One-off dev/debug/maintenance scripts — local convenience only, never
# shared/pushed. Some touch production secrets (service-role keys, payment
# credentials) via process.env; keeping them out of git history entirely
# is safer than trusting every future script here to be equally careful.
scratch/
