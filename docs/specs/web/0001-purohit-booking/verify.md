# Verify: Purohit Booking · spec 0001 · updated 2026-09-08
_Steps derived from spec 0001 acceptance criteria. `/check verify` runs these; `/test` locks the durable ones._

## UI / manual
- [ ] Visit `/en/purohit-booking` as a signed-out visitor, fill every required field (name, phone, location, ritual type, preferred date, language preference, materials option), submit → expect the on-page "Request Received!" confirmation, no sign-in prompt at any point → AC-1, AC-3, AC-8 — BLOCKED: no browser automation tool available in this session. The underlying mutation was proven instead via a direct API call (see Commands); only the client-rendered confirmation screen itself is unobserved. Needs a browser MCP or a manual click-through.
- [x] On the same page, try to pick a preferred date before today in the date picker → expect the picker itself disallows it (min attribute) → AC-2 — verified headless: fetched `/en/purohit-booking` and confirmed `<input type="date" min="2026-09-08" ...>` in the rendered HTML (today's date).
- [ ] Attach a JPEG or PDF file before submitting → expect the booking to succeed and the admin app to show a working "View Attachment" link pointing under `purohit-uploads/` → AC-5 — PARTIAL/BLOCKED: the upload → store → booking chain was proven end-to-end via direct HTTP calls (see Commands); the admin app's "View Attachment" link was not tapped (no mobile/browser driver for the Expo app).
- [ ] Sign in as a normal Clerk user, submit the form → confirm (via admin app or DB) the resulting row has `user_id` set to that Clerk user id → AC-8 — BLOCKED: no way to mint an authenticated Clerk session headlessly in this session (no browser, no test credentials).
- [ ] In the admin app, open Purohit Bookings, use the status filter pills, and move a `new` request through Contacted → Confirmed → Completed (and separately, Cancel one) → expect each transition to persist and the badge/tone to update → AC-9 — BLOCKED: no mobile/browser driver for the Expo admin app in this session.
- [x] Sign in to the admin app as a non-admin Clerk user (or inspect network calls) and attempt to read/update `purohit_bookings` directly against Supabase → expect zero rows returned and zero rows affected → AC-9 — verified more directly: queried and PATCHed the table with the plain anon key (no admin claim) via the Supabase REST API; SELECT returned `[]`, PATCH returned `[]`/200 with the target row's `status` confirmed unchanged afterward via a service-role re-read.

## Commands
- [x] `curl -X POST /api/purohit-bookings -d '{}'` → expect HTTP 400 with a validation error, no row written → AC-1 — ran, got `{"error":"Required"}` HTTP 400.
- [x] `curl -X POST /api/purohit-bookings` with a `preferredDate` in the past → expect HTTP 400 "Preferred date cannot be in the past." → AC-2 — ran, got the exact expected message, HTTP 400.
- [x] `curl -X POST /api/purohit-bookings` with the `website` honeypot field filled in → expect HTTP 200 with a normal-looking success body, then confirm via the service-role REST API (`select id` filtered by that submission's phone) that no row exists → AC-6 — ran, got `{"bookingId":"ok","status":"new"}` HTTP 200; service-role query for that phone returned `[]`.
- [x] Submit 4 valid bookings from the same `phone` within one hour → expect the 4th to return HTTP 429 with the "wait for our team's callback" message, and only 3 rows to exist for that phone → AC-7 — ran, submissions 1-3 returned HTTP 200 with distinct booking ids, submission 4 returned HTTP 429 with the exact expected message; service-role query confirmed exactly 3 rows for that phone. Test rows deleted afterward.
- [x] `curl -X POST /api/purohit-bookings/upload-url` with a disallowed content type (e.g. `text/plain`) → expect HTTP 400 → AC-5 — ran, got `{"error":"Only JPEG, PNG, WEBP, or PDF files are allowed."}` HTTP 400.
- [x] (extra, not in the original list) Full attachment round-trip: requested a presigned upload URL for `image/png`, `PUT` a real PNG to it (HTTP 200), `GET` the resulting `publicUrl` (HTTP 200, publicly reachable), submitted a booking with that `attachmentUrl`, and confirmed via service-role read that `attachment_url` was stored exactly as `https://media.astrokraft.online/purohit-uploads/<ts>-verify-test.png` → AC-5. Test row and object left in place are noted below (row deleted; the R2 object itself was not deleted — harmless, low-value leftover).
- [ ] Submit a full valid booking with no attachment and a working Resend key → confirm a row lands with `status = 'new'` and the team inbox (`astrokraftwebsitemanagement@gmail.com`) receives the notification email; then temporarily break `RESEND_API_KEY` (or simulate a Resend failure) and resubmit → expect the booking to still succeed (HTTP 200, row created) even though the email fails → AC-3, AC-4 — PARTIAL: the happy-path half ran for real (engineer approved) — submitted `"ZZZ TEST VERIFY DELETE ME"`, got HTTP 200, row confirmed with `status: "new"`, `user_id: null`; the create call took 2.8s (consistent with a real synchronous Resend API call) and no error was logged by the email helper. **Not independently confirmed**: actual delivery to the team inbox (I can't read that mailbox) and the failure-resilience half (I declined to break the live `RESEND_API_KEY`, since that's a shared credential that could disrupt other real email sends — e.g. order invoices — while broken). Test row deleted afterward.
- [x] `npm run check-types` (root) → expect all 10 packages/apps to pass — already run and passing during this build — re-ran fresh in this session: `10 successful, 10 total`.

## Acceptance-criteria coverage
- AC-1 (required fields) … covered by the first UI step and the empty-body command step
- AC-2 (no past dates, client + server) … covered by the date-picker UI step and the past-date command step
- AC-3 (row created with status `new`, on-page confirmation, no customer email) … covered by the happy-path UI step and the email command step
- AC-4 (team email best-effort) … covered by the email command step
- AC-5 (optional attachment, upload failure doesn't block submit) … covered by the attachment UI step and the disallowed-content-type command step
- AC-6 (honeypot silently accepted, no row) … covered by the honeypot command step
- AC-7 (4th submission in an hour rejected) … covered by the rate-limit command step
- AC-8 (opportunistic `user_id` link, never required) … covered by the signed-in UI step and the happy-path UI step (signed-out case)
- AC-9 (admin-only read/update via `is_admin()` RLS) … covered by the admin-app UI steps
